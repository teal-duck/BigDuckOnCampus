package com.muscovy.game.enums;


public enum ProjectileDamager {
	// Old documation, now changed as both damage = 2, not 3
	// 0 = damages player, 1 = damages enemy, 3 = damages both
	PLAYER, ENEMY, BOTH;
}
